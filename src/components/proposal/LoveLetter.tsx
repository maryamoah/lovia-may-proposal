'use client';
import { useEffect, useState } from 'react';
import { AnimatePresence, motion } from 'framer-motion';
import { story } from '@/data/story';
import { baseTransition } from './motion';
export function LoveLetter({onReadingChange}:{onReadingChange?:(open:boolean)=>void}){const[open,setOpen]=useState(false);useEffect(()=>onReadingChange?.(open),[open,onReadingChange]);return <section id="letter" data-navigation-theme="light" className={`relative bg-cream px-5 py-[4.5rem] text-espresso sm:px-10 lg:py-24 ${open?'z-10':''}`}><div className="mx-auto max-w-4xl"><div className="text-center">{story.letterIntro.map((l,index)=><motion.p key={l} initial={{opacity:0,y:14}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.8}} transition={{...baseTransition,delay:index*.08}} className="font-serif text-2xl italic text-espresso/70 sm:text-3xl">{l}</motion.p>)}</div><motion.div layout className="paper mt-8 border border-gold/40 p-6 shadow-[0_28px_90px_rgba(27,18,14,.14)] sm:p-10"><div className="text-center"><button onClick={()=>setOpen(v=>!v)} className="wax-seal mx-auto grid h-20 w-20 place-items-center rounded-full bg-rose font-serif text-3xl text-ivory" aria-expanded={open} aria-controls="love-letter-copy">M</button><h2 className="mt-7 font-serif text-[clamp(2.3rem,6vw,4.6rem)] leading-none">My Mommie</h2><p className="mt-4 text-sm uppercase tracking-[.24em] text-gold">{open?'Read slowly':'Press the seal to open'}</p></div><AnimatePresence initial={false}>{open?<motion.article id="love-letter-copy" initial={{height:0,opacity:0}} animate={{height:'auto',opacity:1}} exit={{height:0,opacity:0}} transition={baseTransition} className="overflow-hidden"><div className="mx-auto mt-10 max-w-3xl border-t border-gold/25 pt-8 story-copy text-[1.08rem] leading-8 text-espresso/82 sm:text-[1.28rem] sm:leading-9"><p className="mb-8 text-right font-sans text-sm uppercase tracking-[.18em] text-gold">{story.letterDate}</p>{story.letter.split('\n\n').map((p)=><p key={p} className="mb-7 whitespace-pre-line">{p}</p>)}<p className="mb-8 font-sans text-base text-espresso/70">{story.letterPostscript}</p><button onClick={()=>setOpen(false)} className="mt-2 border border-gold/60 px-5 py-3 font-sans text-xs uppercase tracking-[.24em] text-gold transition hover:bg-gold hover:text-espresso">Close letter</button></div></motion.article>:null}</AnimatePresence></motion.div></div></section>}
